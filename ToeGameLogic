

interactivity

game logic

when the user clicks a button I want that button to be filled in with an x or o
  each cell needs to listen for the click
  could toggle class in some way
  one click for x two clicks for o

  var playerMove = 0
  .on(click, function (){
    if (playerMove ===1) {
      toggle class o
      playerMove = 2
    }
    else if (playerMove === 2){
      reset element
      playerMove = 0
    }else {}
      toggle class x
      PlayerMove= 1;
    }

  })

find a winner
how can some one win?

diagnial
top right mid center bottom left
top left mid center bottom right

vertical
top left mid left bottom left
mid right mid center mid right
top right mid right bottom right

horizontal
top left top mid top right
top center mid center bottom center
bottom right bottom center bottom right

tell if its a tie
if (count >= 9 && .everyting.hasClass(player)){
  return tie
}

function {

  var tieChecker = $('topRow').children()
  $('topRow').children().each()
  $.each(tieChecker, function(index, value){
  console.log(index value)




  })
}


$.square.on click function {


  $submit .on click function {
    thing = thing.removeClass('square')
  }
  $this.attr(thing)
}

submit on click function





reset
$('.newGame').on('click', function(){
  $('.square').empty()
})

reset = all elements.empty()

media query{
  when px gets below 750px toggle smallSquare
}





function gameTime(){
function(){
    if (playerMove === 1){
      $(this).empty();
      $(this).html('<p>o</p>');
      $(this).removeClass('1')
      $(this).addClass('2');
      playerMove = 2;


    }  else if (playerMove === 2){
      $(this).empty();
      $(this).removeClass('1, 2')
      playerMove = 0;

    } else {
      $(this).html('<p>x</p>');
      $(this).addClass('1');
      playerMove = 1;


    }

    checkWinner(playerMove)
  });

}


var winsX = 0
var winsO = 0
var allTimesGamePlayed = 0

I want to check to see whose won. if player one has one then add a count
to the winsX counter. and likewise for wins)

allTimesGamePlayed += 1;

function hisWins(){

if (player === 1){
  winsX += 1
  $('winsHistoryForX').append(winsX)
} else {
  winsO += 1
  $('').append(winsO)
}

}

/*@media (max-width: 750px) {
    $('body').toggleClass('smallSquare');
    }*/

































